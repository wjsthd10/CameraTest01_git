<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    android:id="@+id/rc_cameraButton_lay"
    android:clickable="true"
    android:orientation="vertical"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="75dp"
    android:layout_height="70dp">

    <ImageView
        android:id="@+id/rc_cameraButton_image"
        android:src="@drawable/ic_baseline_select_all_24"
        android:layout_weight="1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <TextView
        android:id="@+id/rc_cameraButton_text"
        android:textStyle="bold"
        android:gravity="center"
        android:text="buttonText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

</LinearLayout>
    <!--        우측 기능 버튼들 리스트( 반/부[리스트 보여줌], 전체[버튼 보여줌/데이터가져가서], 인원지정[리스트 보여줌], 미촬영자[리스트 보여줌] )-->